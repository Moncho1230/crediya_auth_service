server:
  port: 8080
spring:
  application:
    name: "crediya_auth"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
routes:
  paths:
    users: "/api/v1/users"
adapters:
  r2dbc:
    host: "localhost"
    port: 5434
    database: "autentificacion"
    schema: "public"
    username: "postgres"
    password: "1234"
